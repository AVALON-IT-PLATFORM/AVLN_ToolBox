<Window x:Class="Avln.ToolBox.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки"
        Width="520"
        Height="270"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource ToolBoxWindowStyle}">
    <Border Margin="18"
            Background="{StaticResource SurfaceBrush}"
            BorderBrush="{StaticResource AccentBorderBrush}"
            BorderThickness="4"
            CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="10" Opacity="0.22" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="62" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="20,0">
                    <TextBlock Text="Настройки"
                               FontSize="18"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                    <Button HorizontalAlignment="Right"
                            Style="{StaticResource IconButtonStyle}"
                            Click="CloseButton_OnClick"
                            ToolTip="Закрыть">
                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="16" Text="&#xE8BB;" />
                    </Button>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="20,6,20,12">
                <Grid Height="76">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Автообновление" FontSize="13" FontWeight="SemiBold" />
                        <TextBlock Margin="0,4,16,0"
                                   Text="Устанавливать новые версии автоматически, когда Revit закрыт"
                                   Foreground="{StaticResource MutedBrush}"
                                   FontSize="11"
                                   TextWrapping="Wrap" />
                    </StackPanel>

                    <ToggleButton Grid.Column="1"
                                  Width="48"
                                  Height="26"
                                  IsChecked="{Binding AutoUpdate, Mode=TwoWay}"
                                  Checked="AutoUpdate_OnChanged"
                                  Unchecked="AutoUpdate_OnChanged"
                                  VerticalAlignment="Center">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border x:Name="Track" Background="#E4E4E4" CornerRadius="13">
                                    <Ellipse x:Name="Thumb"
                                             Width="20"
                                             Height="20"
                                             Margin="3"
                                             Fill="White"
                                             HorizontalAlignment="Left">
                                        <Ellipse.Effect>
                                            <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2" />
                                        </Ellipse.Effect>
                                    </Ellipse>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="Track" Property="Background" Value="{StaticResource AccentBrush}" />
                                        <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                </Grid>

                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
                    <Grid Height="76">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Папка установки" FontSize="13" FontWeight="SemiBold" />
                            <TextBlock Margin="0,4,16,0"
                                       Text="{Binding InstallBasePath}"
                                       Foreground="{StaticResource MutedBrush}"
                                       FontSize="11"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding InstallBasePath}" />
                        </StackPanel>
                        <Button Grid.Column="1"
                                Content="Изменить"
                                Style="{StaticResource SmallButtonStyle}"
                                Click="ChangePathButton_OnClick"
                                VerticalAlignment="Center" />
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Border>
</Window>
